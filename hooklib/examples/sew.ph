15 # chain;
14 # || {
    turn();
    15 # dc;
};

turn();
let row_1 = [];
15 # || {
	dc();
	row_1.push(mark());
};

new_part();

15 # chain;
14 # || {
    turn();
    15 # dc;
};

turn();
let row_2 = [];
15 # || {
	dc();
	row_2.push(mark());
};

sew(row_1, row_2)